# Unidade III - Orientação a objetos em C++

Esta unidade reúne exemplos introdutórios de orientação a objetos e organização de código em C++. Os programas praticam classes, objetos, atributos, métodos, modificadores de acesso, construtores, membros `static`, funções `inline`, escopo, `extern` e namespaces.
